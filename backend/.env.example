# Required in production — used to sign login tokens
JWT_SECRET=change-me-to-a-long-random-string

# Optional: enables AI-enhanced strengths, gaps, notes, and interview questions
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: SQLite database file path
# DATABASE_PATH=./cvpilot.db
